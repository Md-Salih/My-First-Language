// Array declaration
data-type array-name[array-size];

// Example
int scores[5] = {90, 80, 70, 60, 50};

int main() {
    for (int i = 0; i < 5; i++) {
        std::cout << "Score " << i + 1 << ": " << scores[i] << std::endl;
    }
    return 0;
}

